const {chromium,expect}=require('@playwright/test');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:5173');await p.getByLabel('Start stopwatch',{exact:true}).click();await expect(p.locator('.pause-icon i')).toHaveCount(2);await p.screenshot({path:'artifacts/snapshots/stopwatch-pause-icon.png'});await p.getByLabel('Pause stopwatch',{exact:true}).click();await expect(p.getByLabel('Resume stopwatch')).toBeVisible();await b.close();console.log('Geometric pause icon and pause action verified')})().catch(e=>{console.error(e);process.exit(1)});
