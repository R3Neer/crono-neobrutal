const {chromium,expect}=require('@playwright/test');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:5173');const input=p.getByLabel('Total duration in minutes and seconds');for(const [keys,result] of [['0075','01:15'],['0175','02:15'],['1259','12:59'],['9999','99:59'],['0000','00:00']]){await input.click();await input.pressSequentially(keys);await input.press('Tab');await expect(input).toHaveValue(result)}await input.click();await input.pressSequentially('0190');await p.locator('.timer-control').click();await expect(p.getByLabel('Configured total duration')).toHaveText('02:30');await expect(p.getByLabel('Remaining timer duration')).toHaveValue('02:30');await b.close();console.log('Seconds carry, format limit, zero and direct START verified')})().catch(e=>{console.error(e);process.exit(1)});
