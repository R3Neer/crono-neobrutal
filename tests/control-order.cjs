const {chromium,expect}=require('@playwright/test');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:5173');const buttons=p.locator('.stopwatch-controls button');await expect(buttons.nth(0)).toHaveAttribute('aria-label','Start stopwatch');await expect(buttons.nth(1)).toHaveAttribute('aria-label','Reset stopwatch');expect((await buttons.nth(0).boundingBox()).y).toBeLessThan((await buttons.nth(1).boundingBox()).y);await p.screenshot({path:'artifacts/snapshots/home-final.png'});await b.close();console.log('START above RESET in DOM and layout')})().catch(e=>{console.error(e);process.exit(1)});
